export default function ThemeToggle({ toggleTheme }) {

    return (

        <button
            className="theme-btn"
            onClick={toggleTheme}
        >

            ⚫⚪

        </button>

    );

}